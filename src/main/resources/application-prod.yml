
spring:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
        globally_quoted_identifiers: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://130.162.158.186:3306/googlecalendar
    username: local
    password: adminadmin

  security:
    oauth2:
      resourceserver:
        jwt:
          jwt-set-uri: https://okrbiz.com:8443/realms/springtest/protocol/openid-connect/certs
          issuer-uri: https://okrbiz.com:8443/realms/springtest
      client:
        provider:
          keycloak:
            issuer-uri: https://okrbiz.com:8443/realms/springtest
        registration:
          keycloak:
            client-id: spring-test
            # client-id: client ??
            client-secret: n7vMLTjRG8Ptjwe3DgjcDMcM09TnOceK
            authorization-grant-type: authorization_code
            redirect-uri: '{baseUrl}/login/oauth2/code/keycloak'
            scope:
              - openid
              - profile
              - offline_access


keycloak:
  realm: springtest
  # realm: realm ??
  resource: spring-test
  # resource: client ??
  auth-server-url: https://okrbiz.com:8443
  # auth-server-url: http://??? ???url:port/
  credentials:
    secret: n7vMLTjRG8Ptjwe3DgjcDMcM09TnOceK
  use-resource-role-mappings: true
  bearer-only: true
  ssl-required: external
  principal-attribute: preferred_username

server:
  port: 8000